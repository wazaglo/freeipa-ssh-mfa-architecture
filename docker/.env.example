# Required: IPA admin and Directory Manager passwords
# Copy to .env and set values before running docker compose:
#   cp .env.example .env
#   docker compose up -d
IPA_ADMIN_PASSWORD=changeme
IPA_DM_PASSWORD=changeme
